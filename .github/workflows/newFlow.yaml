name: First action ME
on: [push]

jobs:
    run-shell-commands:
        runs-on: ubuntu-latest
        steps:
            - name: echo a string
              run: echo "Hellloooo"
            - name: Multiline Command
              run: |
               node -v
               npm -v